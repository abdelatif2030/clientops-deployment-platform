# prometheus.yml.j2
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
{% for host in groups['all'] %}
      - targets: ["{{ hostvars[host].ansible_default_ipv4.address }}:9100"]
{% endfor %}